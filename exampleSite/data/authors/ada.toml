# A second author, so the demo site exercises the multi-author path rather than only
# describing it. A co-authored post credits both.
name = "Ada Example"
headline = "Invented the demo co-author"
# A second avatar, so the stacked-avatar group in the byline is actually exercised
# rather than only styled.
# A raster avatar on purpose: Morten's is an SVG, so the two of them exercise both
# branches of image-url.html — the resize path and the pass-through.
image = "images/profile-raster.jpg"
links = [
  { link = "https://example.com" },
]
